{
  "enabled": true,
  "name": "Go 代码规范检查",
  "description": "编写 Go 代码时，检查是否符合项目编码规范",
  "version": "1",
  "when": {
    "type": "preToolUse",
    "toolTypes": [
      "write"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "检查代码规范：1) 函数长度不超过 150 行 2) 嵌套层级不超过 3 层 3) Service 层没有直接调用 Data 层 4) 错误处理使用 kratos/error 包 5) 使用 WithContext(ctx) 传递 context 6) 异步 goroutine 使用 GoSafe 包装 7) 无硬编码配置值。"
  }
}