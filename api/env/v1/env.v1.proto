syntax = "proto3";

package api.env.v1;

option go_package = "github.com/ikaiguang/go-srv-kit/api/env/v1;v1";
option java_multiple_files = true;
option java_package = "api.env.v1";

// Env app 环境
enum Env {
  // UNKNOWN 未知
  UNKNOWN = 0;
  // DEVELOP 开发环境
  DEVELOP = 1;
  // TESTING 测试环境
  TESTING = 2;
  // PREVIEW 预发布 环境
  PREVIEW = 3;
  // PRODUCTION 生产环境
  PRODUCTION = 4;
}