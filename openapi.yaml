# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/apps/protoc-gen-openapi

openapi: 3.0.3
info:
    title: SrvPing API
    description: SrvPing ping服务
    version: 0.0.1
paths:
    /api/v1/ping/{message}:
        get:
            tags:
                - SrvPing
            description: |-
                Ping ping

                 测试PingV1
            operationId: SrvPing_Ping
            parameters:
                - name: message
                  in: path
                  description: 请求消息
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/PingResp'
components:
    schemas:
        PingResp:
            type: object
            properties:
                message:
                    type: string
                    description: 响应消息
            description: PingResp ping响应 ping响应
tags:
    - name: SrvPing
